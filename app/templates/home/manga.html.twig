{% extends 'home/components/layout.html.twig' %}

{% block title %}Project Kakarot - Manga{% endblock %}

{% block content %}
    
    <div class="cards">
        {% for manga in mangas %}
            <div onclick="window.location.href='{{ path('app_manga_view', { 'id': manga.id }) }}'" class="cardView">
                <div class="cardImage" style="background: url({{ asset('img/views/'~ manga.picture) }});background-size: cover;border-radius: 5px 5px 0 0;height: 15rem;box-shadow: inset hsla(0, 0, 0, .2) 0 64px 64px 16px;background-position: center;"></div>
                <div class="cardBody">
                    <h5>{{ manga.title }}</h5>
                </div>
            </div>
        {% endfor %}
    </div>

{% endblock %}
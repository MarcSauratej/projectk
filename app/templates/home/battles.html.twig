{% extends 'home/components/layout.html.twig' %}

{% block title %}Project Kakarot - Batallas{% endblock %}

{% block content %}
    
    <div class="cards">
        {% for battle in battles %}
            <div onclick="window.location.href='{{ path('app_battles_view', { 'id': battle.id }) }}'" class="cardView">
                <div class="cardImage" style="background: url({{ asset('img/views/'~ battle.picture) }});background-size: cover;border-radius: 5px 5px 0 0;height: 15rem;box-shadow: inset hsla(0, 0, 0, .2) 0 64px 64px 16px;background-position: center;" aria-label="Imagen de {{ battle.title }}"></div>
                <div class="cardBody">
                    <h5>{{ battle.name }}</h5>
                </div>
            </div>
        {% endfor %}
    </div>

{% endblock %}
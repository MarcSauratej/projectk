{% extends 'home/components/layout.html.twig' %}

{% block title %}Project Kakarot - Personajes{% endblock %}

{% block content %}
    
    <div class="cards">
        {% for character in characters %}
            <div onclick="window.location.href='{{ path('app_characters_view', { 'id': character.id }) }}'" class="cardView">
                <div class="cardImage" style="background: url({{ asset('/img/views/'~ character.picture) }});" aria-label="Imagen de {{ character.title }}"></div>
                <div class="cardBody">
                    <h5>{{ character.name }}</h5>
                </div>
            </div>
        {% endfor %}
    </div>

{% endblock %}